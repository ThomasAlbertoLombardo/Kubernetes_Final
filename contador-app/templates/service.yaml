apiVersion: v1
kind: Service
metadata:
  name: {{ include "contador-app.fullname" . }}
  labels:
    {{- include "contador-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contador-app.selectorLabels" . | nindent 4 }}
